package greedy;

import java.util.*;

public class Greedy {

	private static Set<Element> set;
	private static Set<Element> solutionSet;
	private static int solutionValue;
	
	public static Set<Element> coinMachine(Set<Element> elements, int salutionValue){
		
		set.addAll(elements);
		solutionSet = new LinkedHashSet<Element>();
		
		while(!set.isEmpty() && !solution(solutionSet)){
			
		}
		return null;
		
		
		
	}

	private static boolean solution(Set<Element> selutionSet) {
		
		return false;
	}
	
	
	
	
	
}
